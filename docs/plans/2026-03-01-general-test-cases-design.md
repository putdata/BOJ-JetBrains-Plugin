# General Test Cases (일반 테스트케이스) 디자인

## 개요

백준 문제와 무관하게 임의의 소스 파일에 대해 커스텀 테스트케이스를 만들어 실행할 수 있는 기능.

## 사용 시나리오

1. **독립 실행**: 백준 문제 없이 내가 작성한 코드에 임의의 입력을 넣고 기대 출력과 비교
2. **예제 보충**: 백준 문제의 공식 예제 외 추가 엣지 케이스 테스트 (기존 커스텀 기능과 별개)

## UI/UX 설계

### 탭 구조

기존 BOJ 우측 도구창에 IntelliJ ContentFactory 탭을 추가하여 2개 탭으로 분리.

- **"백준" 탭**: 기존 `BojToolWindowPanel` 그대로
- **"일반" 탭**: 새로운 `GeneralTestPanel`

탭이 2개이므로 타이틀바 영역에 탭이 자동 표시됨.

### 일반 탭 레이아웃

```
┌─────────────────────────────────┐
│ 📂 Main.java              [↻]  │  ← 현재 바인딩된 파일
├─────────────────────────────────┤
│ ▼ 테스트 1                  [✕] │  ← 접기/펼치기 가능
│ ┌─ 입력 ──────────────────────┐ │
│ │ 3                           │ │
│ │ 1 2 3                       │ │
│ └─────────────────────────────┘ │
│ ┌─ 기대 출력 ─────────────────┐ │
│ │ 6                           │ │  ← 필수
│ └─────────────────────────────┘ │
│                        [▶ 실행] │
│─────────────────────────────────│
│ ▼ 테스트 2                  [✕] │
│ ┌─ 입력 ──────────────────────┐ │
│ │ ...                         │ │
│ └─────────────────────────────┘ │
│ ┌─ 기대 출력 ─────────────────┐ │
│ │ ...                         │ │
│ └─────────────────────────────┘ │
│                        [▶ 실행] │
│─────────────────────────────────│
│         [+ 테스트 추가]          │
├─────────────────────────────────┤
│ [▶ 전체 실행]  [명령어 ▼]       │  ← RunBar
└─────────────────────────────────┘
```

### 동작 방식

- 현재 에디터에서 선택된 파일에 자동 바인딩
- 파일 변경 시 해당 파일의 테스트케이스 목록으로 자동 전환
- 각 테스트는 개별 실행 또는 전체 실행 가능
- 실행 결과는 기존 하단 "BOJ 테스트" 도구창에 표시 (공유)

## 데이터 저장

### 저장 위치

```
.boj/general-cases/{파일명}/
  ├── 테스트 1.in
  ├── 테스트 1.out
  ├── 테스트 2.in
  └── 테스트 2.out
```

- `.in`: 입력 데이터 (plain text)
- `.out`: 기대 출력 (plain text, 필수)
- 파일명은 소스 파일 이름 기준 (경로 아닌 파일명)

### GeneralTestCaseRepository

- `load(fileName)`: 해당 파일의 모든 테스트케이스 로드
- `save(fileName, testName, input, expectedOutput)`: 저장
- `delete(fileName, testName)`: 삭제
- `nextAutoName(fileName)`: 자동 이름 생성 ("테스트 1", "테스트 2", ...)

## 실행 흐름

```
GeneralTestPanel → [전체 실행] 클릭
    ↓
GeneralTestCaseRepository.load(fileName)
    → [(input, expectedOutput), ...]
    ↓
기존 SampleRunService.runSample(SampleCase) 재사용
    ↓
TestResultService에 결과 저장
    (TestCaseKey.General 타입 사용)
    ↓
BojTestResultPanel에 결과 표시
```

### TestCaseKey 확장

```kotlin
sealed class TestCaseKey {
    data class Sample(val index: Int) : TestCaseKey()
    data class Custom(val name: String) : TestCaseKey()
    data class General(val name: String) : TestCaseKey()  // 추가
}
```

## 파일 변경 목록

### 새로 생성

| 파일 | 역할 |
|------|------|
| `GeneralTestPanel.kt` | 일반 탭 UI 패널 |
| `GeneralTestCaseRepository.kt` | .in/.out 파일 기반 저장소 |

### 수정

| 파일 | 변경 내용 |
|------|----------|
| `BojToolWindowFactory.kt` | Content Tab 2개 생성 ("백준", "일반") |
| `TestCaseKey.kt` | `General(name)` sealed class 추가 |
| `BojTestResultPanel.kt` | General 타입 결과 표시 지원 |
| `TestResultService.kt` | General 키 처리 |

### 변경 없음 (재사용)

- `BojToolWindowPanel.kt` — 기존 백준 탭 그대로
- `SampleRunService.kt` / `ProcessSampleRunService.kt` — 실행 엔진
- `OutputComparator.kt` — 결과 비교
- `RunBarPanel.kt` — 명령어 선택 UI
